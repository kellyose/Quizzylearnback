# Steps to run or use the repo

- Run git clone to clone the repo
- Remove the git file using "rm -fr .git"
- Create a .env file and use variables as they were declared in the example.env
- Run npm install
- You are set